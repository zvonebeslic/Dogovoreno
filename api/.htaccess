RewriteEngine On
RewriteBase /api/

# ako je tražena fizička datoteka ili folder, posluži ga
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# sve ostalo ide na index.php (router)
RewriteRule ^ index.php [QSA,L]
